# Dependencies
node_modules/

# OS noise
.DS_Store

# Agent tooling — vendored gstack skills, symlinked to an absolute local path.
# Not portable, and it leaks the machine's home directory.
.claude/
.gstack/

# Firebase CLI project aliases are local to this checkout.
.firebaserc
.firebase/

# Retired Catalyst local state can remain in old worktrees without appearing in
# this Firebase migration.
.catalystrc
catalyst-debug.log
onam-quiz/.catalyst/
firebase-debug.log
database-debug.log

# Secrets
.env
.env.*
!.env.example
